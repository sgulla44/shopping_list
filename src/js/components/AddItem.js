import React from 'react';

export default () => (
	render() {
		return(
			<div>
				<form>
					<input type="text" placeholder="Add something to list?" href="createInput" />
				</form>
				<button onClick={} => console.log('add item')}>+</button>
			</div>
		);
	}
}